name = "cf-mail-relay-demo"
main = "src/worker.ts"
compatibility_date = "2026-05-01"

routes = [
  { pattern = "relay-demo.alexmiller.net", custom_domain = true },
]

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
run_worker_first = true
